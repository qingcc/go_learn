syntax = "proto3";
package example;

message person {    //person 会生成 Person 命名的结构体
    int32 id = 1;
    string name = 2;
}

message all_person {    //all_person 会生成 AllPerson 的驼峰命名的结构体
    repeated person Per = 1;
}

